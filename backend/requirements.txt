# Travel AI Chatbot - Backend Dependencies
# Install with: pip install -r requirements.txt

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0      # ASGI server to run FastAPI
pydantic>=2.8.0                 # Data validation for request/response models

# --- LLM and model loading ---
torch>=2.1.0                    # PyTorch (GPU support included)
transformers>=4.45.0            # HuggingFace Transformers
peft>=0.12.0                    # LoRA adapter support
bitsandbytes>=0.43.0            # 4-bit quantization (QLoRA)
accelerate>=0.34.0              # Multi-GPU and device management
huggingface-hub>=0.24.0         # Download models from HuggingFace Hub

# --- Faster training/inference (optional but recommended) ---
# unsloth                       # Uncomment if you installed unsloth during training
# If unsloth is not installed, the backend falls back to standard HuggingFace

# --- Semantic similarity scoring ---
sentence-transformers>=3.0.0    # all-MiniLM-L6-v2 for match score calculation
